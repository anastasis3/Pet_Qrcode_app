use folder images in pet_qrcode project where you can find pictures of design of every screen designes by stitch Using the Stitch mockups as the source of truth, recreate every screen using Jetpack Compose.

Important:

Do not implement business logic.

Do not create ViewModels.

Do not create API calls.

Use fake preview data.

Build pixel-perfect UI.

Create:

LoginScreen

RegisterScreen

HomeScreen

AddPetScreen

PetProfileScreen

QRCodeScreen

LostPetsScreen

PublicPetProfileScreen

ScanHistoryScreen

Every screen must exactly match the Stitch design.

Use reusable components from the Design System.

Create @Preview for every screen.
# Pet_Qrcode_app
