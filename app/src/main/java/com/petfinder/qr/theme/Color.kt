package com.petfinder.qr.theme

import androidx.compose.ui.graphics.Color

// ── Primary ──────────────────────────────────────────────────────────────────
val Primary = Color(0xFF416182)
val OnPrimary = Color(0xFFFFFFFF)
val PrimaryContainer = Color(0xFFAFCFF5)
val OnPrimaryContainer = Color(0xFF395879)
val PrimaryFixed = Color(0xFFD0E4FF)
val PrimaryFixedDim = Color(0xFFA9C9EF)
val OnPrimaryFixed = Color(0xFF001D34)
val OnPrimaryFixedVariant = Color(0xFF294969)

// ── Secondary ──────────────────────────────────────────────────────────────
val Secondary = Color(0xFF65587C)
val OnSecondary = Color(0xFFFFFFFF)
val SecondaryContainer = Color(0xFFE3D3FD)
val OnSecondaryContainer = Color(0xFF66597D)
val SecondaryFixed = Color(0xFFEBDDFF)
val SecondaryFixedDim = Color(0xFFCFC0E9)
val OnSecondaryFixed = Color(0xFF201635)
val OnSecondaryFixedVariant = Color(0xFF4D4163)

// ── Tertiary ─────────────────────────────────────────────────────────────────
val Tertiary = Color(0xFF73584D)
val OnTertiary = Color(0xFFFFFFFF)
val TertiaryContainer = Color(0xFFE8C5B7)
val OnTertiaryContainer = Color(0xFF6A5046)
val TertiaryFixed = Color(0xFFFFDBCD)
val TertiaryFixedDim = Color(0xFFE2BFB2)
val OnTertiaryFixed = Color(0xFF2A170F)
val OnTertiaryFixedVariant = Color(0xFF5A4137)

// ── Error ────────────────────────────────────────────────────────────────────
val Error = Color(0xFFBA1A1A)
val OnError = Color(0xFFFFFFFF)
val ErrorContainer = Color(0xFFFFDAD6)
val OnErrorContainer = Color(0xFF93000A)

// ── Surface & Background ─────────────────────────────────────────────────────
val Background = Color(0xFFFBF9F6)
val OnBackground = Color(0xFF1B1C1A)
val Surface = Color(0xFFFBF9F6)
val OnSurface = Color(0xFF1B1C1A)
val OnSurfaceVariant = Color(0xFF43474D)
val SurfaceBright = Color(0xFFFBF9F6)
val SurfaceDim = Color(0xFFDBDAD7)
val SurfaceTint = Color(0xFF416182)

val SurfaceContainer = Color(0xFFEFEEEB)
val SurfaceContainerHigh = Color(0xFFEAE8E5)
val SurfaceContainerHighest = Color(0xFFE4E2DF)
val SurfaceContainerLow = Color(0xFFF5F3F0)
val SurfaceContainerLowest = Color(0xFFFFFFFF)
val SurfaceVariant = Color(0xFFE4E2DF)

// ── Outline ──────────────────────────────────────────────────────────────────
val Outline = Color(0xFF73777E)
val OutlineVariant = Color(0xFFC3C7CE)

// ── Inverse ────────────────────────────────────────────────────────────────────
val InverseSurface = Color(0xFF30312F)
val InverseOnSurface = Color(0xFFF2F0ED)
val InversePrimary = Color(0xFFA9C9EF)

// ── Component-specific tokens ──────────────────────────────────────────────────
val StatusLost = Color(0xFFF6D2C4)
val InputBackground = Color(0xFFFFF3E6)
val CardBorder = Color(0xFFEAEAEA)
val SearchBackground = Color(0xFFFFF3E6)

/** Gradient used on primary pill buttons (Stitch mockup). */
val PrimaryButtonGradientStart = Color(0xFFAFCFF5)
val PrimaryButtonGradientEnd = Color(0xFF95BCEB)
val PrimaryButtonGradientEndAlt = Color(0xFF8FB8E6)

/** Gradient used on card action buttons in Lost Pets screen. */
val CardActionGradientStart = Color(0xFFAFCFF5)
val CardActionGradientEnd = Color(0xFF9DC1E8)

// ── Soft tints (Stitch dashboard, profile & public recovery) ──────────────────
/** Home dashboard "All Safe" stat card background. */
val StatCardBlue = Color(0xFFE7EEF7)
/** Home dashboard "QR Scans" stat card background. */
val StatCardPurple = Color(0xFFF1ECF8)

/** Public profile soft action buttons. */
val SoftActionBlue = Color(0xFFBBD4F1)
val SoftActionPurple = Color(0xFFD9CEF3)
val SoftActionPeach = Color(0xFFF1CEC0)

/** Contact-info row icon circles. */
val IconCircleBlue = Color(0xFFC7DBF5)
val IconCirclePurple = Color(0xFFE6DAF7)
val IconCirclePeach = Color(0xFFF1D9CE)

/** Map preview placeholder gradient stops. */
val MapPreviewGreen = Color(0xFFCFE3D8)
val MapPreviewBlue = Color(0xFFBFD6E4)
val MapPreviewSand = Color(0xFFEDE4C9)

/** Public profile "About Me" callout background. */
val AboutMeBg = Color(0xFFF7E7E0)

/** QR code panel inner surface. */
val QrPanelBg = Color(0xFFEDEDED)
